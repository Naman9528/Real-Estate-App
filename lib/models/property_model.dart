class Property {
  final String title;
  final String price;
  final String imageUrl;
  // Add any other fields you need, like location, type, etc.

  const Property({
    required this.title,
    required this.price,
    required this.imageUrl,
  });
}